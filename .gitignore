node_modules/
dist/
dist-ssr/
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Turbo C++ toolchain -------------------------------------------------
# The compiler is never repo content. Users supply their own install disks at
# runtime and they are unpacked in the browser (see README). This just keeps a
# local copy from being committed by accident.
*.7z
*.img
vendor/

# js-dos runtime, staged out of node_modules by tools/copy-emulator-assets.mjs
public/emulators/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
